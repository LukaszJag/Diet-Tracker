How to split data to different tables:


General fields for all workout types:

Must have:
    -> ID
    -> Name
    -> Location
    -> Day_date ---> 2025-11-14

Optional:

    enter_workout_area_full_time_date
    start_workout_time_full
    end_workout_time_full

The main workout types and its field


It will be one table for summary all workouts
and
It will be many tables for specific workout, for example:

Gym and play on field will have own tables



